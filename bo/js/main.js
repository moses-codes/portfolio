/*let drink = document.querySelectorAll('.drinkSelection')
let milk = Array.from(document.querySelectorAll('.drinkAdd milks'))

Array.from(drink).forEach(e => addEventListener('click', function (e) {
    let drinkChoice = document.querySelector('#drinkChoice')
    console.log(e.srcElement.innerText, 'kijiji')
    drinkChoice.innerText = e.srcElement.innerText
    e.stopImmediatePropagation()
    e.preventDefault()
})
)

milk.forEach(e => addEventListener('click', function () {
    let milkChoice = document.querySelector('#milkChoice', 'kijojo')
    milkChoice = e.srcElement.innerText
    e.stopImmediatePropagation()
    e.preventDefault()
})
)*/